export const ProjectTitle = ({text}) =>{
    return(
        <p className=" text-sm text-black">{text}</p>
    )
}

export const TaskCount = ({text}) =>{
    return(
        <p className=" text-xs text-gray-400">{text}</p>
    )
}